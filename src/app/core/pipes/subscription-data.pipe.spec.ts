import { SubscriptionDataPipe } from './subscription-data.pipe';

describe('SubscriptionDataPipe', () => {
  it('create an instance', () => {
    const pipe = new SubscriptionDataPipe();
    expect(pipe).toBeTruthy();
  });
});
